
const FINDMODE = 1;
const ATTACKMODE = 0;
const VERTICAL = 0;
const HORIZONTAL = 1;

let mode = FINDMODE;
let lastX: number | null = null;
let lastY: number | null = null;

export const initIA = () => {
}

const shoot = () => {
    if (mode == FINDMODE) {
        // jogar em uma cordenada x,y aleatoria valida
    }
    else {
        // Detectar se em qual orientação está
        // Afundar o navio
    }
}